# Default (dev) configuration
# These values are overridden by environment-specific files and system properties.

api.base.url=https://jsonplaceholder.typicode.com
api.connection.timeout=10000
api.read.timeout=30000
